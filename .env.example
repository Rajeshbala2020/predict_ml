DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/ml_service

# Optional: when Yahoo has no series for a symbol, try these before failing (see service.py).
# Stooq: open https://stooq.com/q/d/?s=SYMBOL&get_apikey — captcha gives a CSV URL containing apikey=...
# ML_STOOQ_API_KEY=xxxxxxxx
# Alpha Vantage free key: https://www.alphavantage.co/support/#api-key (rate limits apply)
# ML_ALPHA_VANTAGE_KEY=xxxxxxxx
